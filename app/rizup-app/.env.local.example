# ── Supabase ─────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://pcysqlvvqqfborgymabl.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
# Webhook などサーバー側のみ。クライアントには絶対に露出しないこと。
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# ── Stripe（課金） ───────────────────────────────────────
# Stripe Dashboard → Developers → API keys から取得
STRIPE_SECRET_KEY=sk_live_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx

# Webhook Endpoint の Signing Secret
# Stripe Dashboard → Developers → Webhooks → 対象エンドポイント → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_xxx

# 商品 → Price ID（Stripe で月額サブスクを作って発行）
STRIPE_PRO_PRICE_ID=price_xxx
STRIPE_PREMIUM_PRICE_ID=price_xxx

# ── 通知（任意）──────────────────────────────────────────
# Web Push（公開鍵はクライアントに露出して OK）
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:a1d.70th@gmail.com

# ── Claude API（AI要約）─────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-xxx

# ── Vercel Cron 用シークレット（任意）── ─────────────────
CRON_SECRET=anything-random

# ============================================================
# 設定後の確認手順
# 1. Vercel → Project → Settings → Environment Variables に同じ値を登録
# 2. Stripe Dashboard → Webhooks → Add endpoint
#    URL: https://rizup-app.vercel.app/api/stripe/webhook
#    Events: checkout.session.completed /
#            customer.subscription.updated /
#            customer.subscription.deleted
# 3. /pricing からテスト購入（テスト環境なら sk_test_ / pk_test_ を使用）
# ============================================================
